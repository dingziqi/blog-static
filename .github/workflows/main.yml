name: hexo

on:
  push:
    branches:
      - hexo2

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: init
      uses: actions/checkout@v1

    - name: checkGit
      run: |
        git config user.name dingziqi
        git config user.email dingziqi_xtu@sina.com

    - name: setup node
      uses: actions/setup-node@v1
      with:
        node-version: '8.x'

    - name: install
      uses: nuxt/actions-yarn@master
      with:
        args: install

    - name: Deploy
      run: |
        npx hexo clean
        npx hexo g
        cd ./docs
        git init
        echo after init
        git config user.name dingziqi
        git config user.email dingziqi_xtu@sina.com
        git add -A
        git commit -m 'action commit'
        echo before push
        git push -u -f https://github.com/dingziqi/blog-static.git HEAD:gh-pages

